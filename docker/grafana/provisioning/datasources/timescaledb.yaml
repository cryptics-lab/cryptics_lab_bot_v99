apiVersion: 1

datasources:
  - name: TimescaleDB
    type: postgres
    url: timescaledb:5432
    user: postgres
    database: cryptics
    jsonData:
      sslmode: "disable"
      postgresVersion: 1500
      timescaledb: true
    secureJsonData:
      password: postgres
    editable: true